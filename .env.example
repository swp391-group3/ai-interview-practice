# PostgreSQL
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=
DB_PORT=

# JD extraction (model is an example, not a runtime default).
LLM_PROVIDER=openai_compatible
LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
LLM_API_KEY=
LLM_MODEL=gemini-3.1-flash-lite
LLM_TIMEOUT=30s
LLM_MAX_RETRIES=1
