import { RoutePlaceholder } from "@/components/feedback/route-placeholder";
export default function Page() {
  return (
    <RoutePlaceholder
      title="Log in"
      description="Sign-in will be connected after the backend session contract is finalized."
    ></RoutePlaceholder>
  );
}
