import { RoutePlaceholder } from "@/components/feedback/route-placeholder";
export function ProfileOverview() {
  return (
    <RoutePlaceholder
      title="Profile"
      description="Manage your candidate profile once account integration is available."
    />
  );
}
