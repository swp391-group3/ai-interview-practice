import { RoutePlaceholder } from "@/components/feedback/route-placeholder";
export function BillingOverview() {
  return (
    <RoutePlaceholder
      title="Billing and credits"
      description="Manage interview credits, packages, checkout and transaction history through the future backend billing integration."
    />
  );
}
