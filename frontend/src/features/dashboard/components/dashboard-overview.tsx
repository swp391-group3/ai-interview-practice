import { RoutePlaceholder } from "@/components/feedback/route-placeholder";
export function DashboardOverview() {
  return (
    <RoutePlaceholder
      title="Dashboard"
      description="Your interview activity and next steps will appear here once session data is connected."
    />
  );
}
